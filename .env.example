# URL to a ZIP backup (output of the in-app Export). When set, the empty
# gallery shows a "Load demo gallery" button that fetches this archive and
# imports it. Leave unset for self-hosted builds.
VITE_DEMO_BACKUP_URL=
